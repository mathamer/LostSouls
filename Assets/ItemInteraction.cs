using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemInteraction : MonoBehaviour
{
    // public GameObject itemObject;
    // public GameObject npcObject;

    // private Item itemScript;
    // private CharacterManager npcScript;

    // void Start()
    // {
    //     itemScript = itemObject.GetComponent<Item>();
    //     npcScript = npcObject.GetComponent<CharacterManager>();
    // }

    // void Update()
    // {
    //     if (Vector3.Distance(itemObject.transform.GetChild(0).position, GameObject.FindGameObjectWithTag("Player").transform.position) < 3)
    //     {
    //         // Assuming you want the interaction to happen when the item is picked up
    //         itemScript.OnPickedUp();
    //         npcScript.ChangeToSprite2();
    //     }
    // }
}
